import { Action, ActionReducerMap } from '@ngrx/store';
import { InjectionToken } from '@angular/core';
//import * as fromLayout from '@example-app/core/reducers/layout.reducer';

export const MAP_STORE: string = 'mapStore';
